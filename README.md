# capstone-project-part-2
# data processing
    * data leakage
    * class imbalance
# model development 
    * regression
    * binary classification
# evalution

explanations:
             y_reg represents continuous numeric values for regression
             y_clf binary variable balanved distribution
# lable encoding :
              implies an inherent ranking
              one hot encoding creates independent binary flag from assuming a false mathematical order
# metric formula
              precision=tp/tp+fp
              recall=tp/tp+fn
# metric priority for player performance:
              whether a player delivers an above median performance,pricision is more critical
              recall is more vital if the object is talent filtration
#auc value:
              measure the model's ability to discriminate between classes
              an auc value of x means x% probability that the model will rank randomly chosen positive instance (high                      performance)higher than a randomly chosen negative instance (low performance)
# decision thersold analysis :
              * optical thershold
              * key metric priority
              * threshold tuning
# statistical reliability :
              * mean different
              * 95% confidence interval
              * reliability conclusion
  
             
